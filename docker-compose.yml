services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: llama_herd_studio
      POSTGRES_USER: llama
      POSTGRES_PASSWORD: llama
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: